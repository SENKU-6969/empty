// Firebase config template
// ---------------------------------------------------------------
// 1. Copy this file to firebase-config.js  (same directory)
// 2. Replace each placeholder with values from your Firebase project:
//    Firebase console → Project Settings → Your apps → SDK setup
// 3. firebase-config.js is gitignored — never commit it.
// ---------------------------------------------------------------
// Note: this is loaded as a plain <script> tag (not an ES module),
// so FIREBASE_CONFIG is a global variable consumed by firebase.js.
// Do NOT add an export statement.
// ---------------------------------------------------------------

const FIREBASE_CONFIG = {
    apiKey: "YOUR_API_KEY_HERE",
    authDomain: "YOUR_PROJECT.firebaseapp.com",
    projectId: "YOUR_PROJECT",
    storageBucket: "YOUR_PROJECT.firebasestorage.app",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID",
    measurementId: "YOUR_MEASUREMENT_ID"
};
